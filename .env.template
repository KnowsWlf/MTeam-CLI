# ── M-Team 多账户（数字后缀，从 _1 开始连续编号）───────────────────
# 凭证与 API key、Telegram/飞书 按账户独立。SMTP 服务器是全局的，收件人按账户。
#
# 凭证（相互独立，可只配其一）：
#   保活（login/run/schedule）需要 USERNAME + PASSWORD + TOTP_SECRET
#   数据查询（profile/search/...）需要 API_KEY（M-Team 控制台「实验室」生成）

# ===== 账户 1 =====
MTEAM_USERNAME_1=user1
MTEAM_PASSWORD_1=pass1
MTEAM_TOTP_SECRET_1=totp1
MTEAM_API_KEY_1=apikey1

# 账户 1 的通知（每个渠道各自 opt-in；都留空 = 该账户不发通知）
# Telegram（token + chat_id 都填才启用）
NOTIFY_TELEGRAM_TOKEN_1=
NOTIFY_TELEGRAM_CHAT_ID_1=
# 飞书（webhook token）
NOTIFY_FEISHU_TOKEN_1=
# SMTP 收件人（SMTP 服务器配置见下方全局区）
NOTIFY_SMTP_TO_1=

# ===== 账户 2（按需取消注释）=====
# MTEAM_USERNAME_2=user2
# MTEAM_PASSWORD_2=pass2
# MTEAM_TOTP_SECRET_2=totp2
# MTEAM_API_KEY_2=apikey2
# NOTIFY_TELEGRAM_TOKEN_2=
# NOTIFY_TELEGRAM_CHAT_ID_2=
# NOTIFY_FEISHU_TOKEN_2=
# NOTIFY_SMTP_TO_2=

# ── SMTP 服务器（全局，所有账户共享；收件人按账户 NOTIFY_SMTP_TO_<n>）───
# 465=SSL，587=STARTTLS。密码用授权码而非登录密码。
# NOTIFY_SMTP_FROM 只填纯邮箱地址（如 user@foxmail.com）。程序已自动加
# "MTeam-CLI <addr>" 显示名；如果这里再写显示名，会被双重包装导致 QQ 502。
NOTIFY_SMTP_HOST=
NOTIFY_SMTP_PORT=465
NOTIFY_SMTP_USER=
NOTIFY_SMTP_PASSWORD=
NOTIFY_SMTP_FROM=
NOTIFY_SMTP_USE_TLS=true

# ── 高分新片摘要（拼进签到通知；每账户隔离）──────────────────
# 每个开了开关的账户用「自己的 api_key + 自己的配置」独立拉取自己的 digest。
# 配置三级优先级：命令行 > 账户覆盖(MTEAM_DIGEST_*_i) > 全局默认(无后缀)。
#
# 账户级开关（默认 false）：开启后该账户签到通知尾部附当天高分新片。
# 注意：拉取用该账户的 api_key，所以该账户必须配了 MTEAM_API_KEY_i。
# MTEAM_DIGEST_ENABLED_1=true

# ↓ 全局默认（无后缀）：所有账户未单独覆盖时继承这些值。
# IMDB 评分下限
MTEAM_DIGEST_MIN_IMDB=8.0
# 资源类型（search mode，逗号分隔）
# 可选: movie(电影) / tvshow(电视剧) / music(音乐) / adult(成人)
#        / waterfall(瀑布流) / rss / rankings(排行) / all(全部) / normal(综合)
# 注意: music/adult 等非影视类型没有 IMDB 评分，会被评分过滤滤光，不适合本功能。
MTEAM_DIGEST_TYPES=movie,tvshow
# 发布时间窗口（小时）
MTEAM_DIGEST_HOURS=24
# 最多列出条数
MTEAM_DIGEST_LIMIT=10

# ↓ 账户级覆盖（带后缀 _i，全部可选；不配则继承上面的全局默认）。
# 例：账户 1 只要电影、门槛降到 7.5；账户 2 保持全局默认即可不写。
# MTEAM_DIGEST_TYPES_1=movie
# MTEAM_DIGEST_MIN_IMDB_1=7.5
# MTEAM_DIGEST_HOURS_1=48
# MTEAM_DIGEST_LIMIT_1=5

# ── 站点 / 浏览器（一般无需改）────────────────────────────────
# MTEAM_BASE_URL=https://zp.m-team.io
# 数据 API 默认走 api.m-team.cc；如需切回 .io 取消注释：
# MTEAM_API_BASE_URL=https://api.m-team.io/api
# MTEAM_HEADLESS=true
# MTEAM_TIMEOUT_MS=60000

# ── 定时调度（全局）───────────────────────────────────────────
# MTEAM_SCHEDULE_WINDOW=09:00-11:00
# MTEAM_SCHEDULE_PRE_DELAY_RANGE=10-300
# MTEAM_SCHEDULE_HEARTBEAT_HOURS=1
